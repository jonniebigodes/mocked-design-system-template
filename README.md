# Mocked Design Syste
This is a mocked design system package used for testing different UI libraries with the MCP. It provides basic UI components and styles to simulate a real design system without the complexity of a full implementation.
